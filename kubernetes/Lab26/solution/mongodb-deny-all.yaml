apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-mongo-ingress
  namespace: default
spec:
  podSelector:
    matchLabels:
      type: backend    # MongoDB pods
  policyTypes:
    - Ingress
