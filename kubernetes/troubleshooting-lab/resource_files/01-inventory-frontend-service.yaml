# BUG: Selector mismatch (app: inventory-ui instead of app: inventory-frontend)
apiVersion: v1
kind: Service
metadata:
  name: inventory-frontend-svc
  namespace: break-fix-lab
spec:
  type: NodePort
  selector:
    app: inventory-ui 
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30000
